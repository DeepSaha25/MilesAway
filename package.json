{
  "name": "runsphere-railway-root",
  "private": true,
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "start": "npm start --prefix backend",
    "build": "npm ci --prefix backend",
    "test": "npm test --prefix backend"
  }
}
